name: CI

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    name: Test and build
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Check lint
        run: python -m ruff check .

      - name: Check formatting
        run: python -m ruff format --check .

      - name: Run tests
        env:
          DATABASE_PATH: /tmp/collision-repair-order-summarizer-api-test.db
          OPENAI_API_KEY: ""
        run: python -m pytest -q

      - name: Build Docker image
        run: docker build -t collision-repair-order-summarizer-api:ci .
